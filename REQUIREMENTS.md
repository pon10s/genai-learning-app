# REQUIREMENTS.md — 要件定義

> 「何を作るか」の正本。迷ったらここに戻る。仕様を増やすときは必ずここを更新してから。

## 1. 目的・背景

- 先生は生成AIコンサル（ソフト開発への生成AI適用を支援）。技術には強くない。
- 生成AI / LLM はトレンドの移り変わりが速く、重要情報のキャッチアップが大変。
- 「記事を読む → クイズで理解を定着」のサイクルで、効率よく学びたい。
- あわせて、Git・環境・開発の進め方といった**開発の基礎**も学びたい。
- このアプリ作り自体を、AI駆動開発の実地練習にする。

## 2. ターゲットユーザー

- 先生ひとり（個人用）。
- 開発初心者。専門用語は噛み砕いた説明が必要。

## 3. 学習の柱（コンテンツのテーマ）

1. **生成AI / LLM の最新トレンド** … 新モデル・新機能・話題の手法
2. **AI駆動開発の実践知** … プロンプト設計、Claude Code等ツールの使い方、開発への適用ノウハウ
3. （補助）**開発の基礎知識** … Git、環境構築、開発の進め方

## 4. コア機能（What）

### 4.1 学習プレイヤー（アプリ本体）
- クイズの一覧／カテゴリ・難易度での絞り込み
- クイズ出題（4択を基本。○×や穴埋めも将来検討）
- 正誤判定 → **解説の表示**（ここが学習の肝。出典URLも表示）
- 進捗・正答率の記録（localStorage、端末ごと）
- 復習機能（間違えた問題の再出題）
- 用語をクリックすると用語集の説明が見られる（将来）

### 4.2 コンテンツ生成（Claude Code 側ワークフロー）
- 「今日のキャッチアップ」で、Claudeが最新記事の**候補**を提示
- 先生が選ぶ → Claudeが記事を**要約** → **クイズ（問題・選択肢・正解・解説・出典）**をJSONで生成
- 生成物は `data/` に保存。手順は `docs/WORKFLOW.md` に従う。

## 5. データの形（クイズJSONの想定スキーマ）

**「1記事 ＝ 読み物（1〜2分）＋ その記事のクイズ」** を1ファイルにまとめる（記事単位）。

```json
{
  "id": "art-xxxx",
  "category": "trend | ai-dev | basics",
  "title": "画面見出し用の短いタイトル",
  "readMinutes": 2,
  "source": { "title": "記事タイトル", "url": "https://...", "date": "2026-06" },
  "summary": [
    "段落1：やさしい導入（1〜2分で読める全体まとめ）",
    "段落2：要点",
    "段落3：なぜ重要か／用語のかみ砕き"
  ],
  "questions": [
    {
      "question": "問題文",
      "choices": ["選択肢A", "選択肢B", "選択肢C", "選択肢D"],
      "answerIndex": 0,
      "difficulty": 2,
      "explanation": "なぜそれが正解か／なぜ他が違うか。学びになる解説。"
    }
  ]
}
```
- **確定（フェーズ4で改訂）**。当面は **4択のみ**。`category` は `trend`/`ai-dev`/`basics`。`difficulty` は 1〜3。
- **summary は段落の配列**。合計300〜500字程度（1〜2分で読める）。先に読んでから解く設計。
- **選択肢は“紛らわしい良問”に**：記事を理解していないと選べない、もっともらしいダミーにする
  （「パスワード」等の明らかなジョーク選択肢は避ける）。

### 保存場所とまとめ方
- 記事ファイルは **`src/data/articles/*.json`**（1ファイル＝1記事）。
  ※ 公開サイトには `src/` 配下しか載らないため、データは src/ の中に置く。
- 読み込む一覧は **`src/data/manifest.json`** の `articles` 配列で管理。
  記事を足したら、ファイルを `src/data/articles/` に置き、manifest に1行追加する。

### 出題の仕方（アプリ側）
- ホームの「今日のクイズ」で、プールから**ランダムに1記事**を選ぶ（直前と同じはなるべく避ける）。
- 「読む → クイズ → 結果」。結果から「別の記事に挑戦（ランダム）」で次へ。

## 6. データ保存（localStorage に持つもの）

- 各問題の: 出題回数 / 正解回数 / 最終回答日時 / 間違えフラグ
- お気に入り（あとで復習したい問題）
- 設定（テーマ、表示順など。将来）

## 7. 非機能・制約

- フレームワークなし（バニラ HTML/CSS/JS）。ビルド不要。
- 依存ライブラリ原則なし。
- GitHub Pages で公開（静的サイト）。最初から公開前提。
- localStorage は端末ごと。**端末間同期は当面しない**（将来フェーズで検討）。
- アプリ本体にAPIキー／AI呼び出しを埋め込まない（B方式）。

## 8. スコープ外（今はやらない）

- ログイン・複数ユーザー
- サーバー／DB（クラウド同期は後フェーズで検討）
- アプリからのAI API直叩き（A方式）
- 画像の本格実装（必要になったら別途検討）

## 9. 用語（このプロジェクト内の言葉の定義）

- **B方式** … クイズ生成を Claude Code が手動ワークフローで担う方式（APIキー不要）。
- **学習プレイヤー** … `src/` の静的アプリ。出題と進捗管理だけを行う。
- **コンテンツ生成エンジン** … Claude Code 自身。記事探し→要約→クイズ化を担う。
