# CLAUDE.md — AIへの指示ファイル

> このファイルは、Claude（AI）が毎セッションの最初に読む「約束ごと」です。
> 指示がブレないように、ここを常に最新に保ちます。

## プロジェクト概要

**生成AIキャッチアップ学習アプリ**

生成AI / LLM の最新トレンドや、AI駆動開発・開発基礎の知識を、
「記事を読む → クイズで定着」という流れで学べる個人用Webサイト。

ユーザー（以下「先生」）は生成AIコンサル。技術そのものには強くないため、
このアプリ自体が「学習教材」であると同時に「Git・デプロイ・開発の進め方の実地練習場」になる。

## このアプリの心臓部：役割分担（最重要・必読）

このアプリは2つの役割がはっきり分かれている。混同しないこと。

```
Claude Code（あなた）= コンテンツ生成エンジン
   └ Web検索で記事候補を出す → 先生が選ぶ → 要約 → クイズJSONを data/ に書き出す
静的アプリ（src/ のバニラHTML/JS）= 学習プレイヤー
   └ data/ のJSONを読む → クイズ出題 → 進捗・正答率を localStorage に保存
```

- **APIキーは使わない。** クイズ生成は「あなた（Claude Code）」が手動ワークフローで行う。
  アプリ本体にAI呼び出しコードを埋め込まないこと。
- コンテンツ生成の具体手順は **必ず `docs/WORKFLOW.md` に従う**。

## 最重要ルール（必ず守ること）

1. **作業前に必ず `docs/PHASES.md` を読む** — 今どのフェーズ・どのタスクかを確認する。
2. **各タスクの最後に必ずセルフレビューを行う** — `docs/REVIEW.md` のチェックリストを実施し、結果をPHASESに残す。
3. **作業後に必ず記録を残す** — `CHANGELOG.md` に追記し、`docs/PHASES.md` のタスク状態を更新する。
4. **要件は `REQUIREMENTS.md` が正** — 迷ったらここを参照。勝手に仕様を増やさない。
5. **デザインは `docs/DESIGN.md` に従う** — 色・フォント・トーンを勝手に変えない。
6. **不明点は推測で進めず、先生（ユーザー）に聞く** — 特に仕様の分岐は確認する。
7. **1タスク = 1まとまり** — 大きく作りすぎず、確認しながら進める。
8. **学びの機会を逃さない** — 先生は開発初心者。新しい用語・操作（Git等）が出たら、
   ひとことで「これは何か・なぜやるか」を添える。説明は短く、専門用語は噛み砕く。

## 技術方針

- **フレームワークなし**（バニラ HTML / CSS / JavaScript）。ビルド不要、ダブルクリックで開ける。
- **データ保存は localStorage**（進捗・正答率・お気に入り。端末ごとに別管理）。
- **クイズデータは `src/data/quizzes/` のJSONファイル**（あなたがワークフローで生成）。
- **公開は GitHub Pages**（最初から公開前提。Git/デプロイ学習を兼ねる）。
- **依存ライブラリは原則なし**。必要になったら理由を添えて提案する。
- **1ファイルが大きくなりすぎないよう** 適度に分割する。

## やってはいけないこと

- 要件にない機能を勝手に追加する
- アプリ本体にAPIキーやAI呼び出しを埋め込む（B方式の原則を壊す）
- クイズの事実誤認を放置する（生成時は出典URLを必ず残し、検証する）
- 記録ファイル（CHANGELOG / PHASES）の更新を忘れる
- セルフレビューを飛ばす

## ファイルの役割早見表

| ファイル | 役割 |
|---|---|
| `CLAUDE.md` | このファイル。AIへの約束ごと |
| `REQUIREMENTS.md` | 要件定義（何を作るか） |
| `CHANGELOG.md` | 変更履歴（何を変えたか） |
| `docs/PHASES.md` | フェーズ・タスク管理（今どこにいるか） |
| `docs/DESIGN.md` | デザイン方針（見た目のブレ防止） |
| `docs/WORKFLOW.md` | コンテンツ生成の手順（記事探し→クイズ化） |
| `docs/REVIEW.md` | セルフレビューのチェックリスト |
| `docs/GLOSSARY.md` | 用語集（先生の学習メモ。出てきた言葉を貯める） |
| `src/` | 学習プレイヤー本体のコード |
| `src/data/quizzes/` | クイズデータ（JSON）。公開サイトに載るよう src/ 内に置く |
| `src/data/manifest.json` | アプリが読むクイズファイルの一覧 |

## セッション開始時の手順（AI向け）

1. この `CLAUDE.md` を読む
2. `docs/PHASES.md` で現在のフェーズ・進行中タスクを確認
3. 必要なら `REQUIREMENTS.md` `docs/DESIGN.md` `docs/WORKFLOW.md` を参照
4. 作業する
5. `docs/REVIEW.md` のチェックでセルフレビュー
6. `CHANGELOG.md` と `docs/PHASES.md` を更新して終わる
